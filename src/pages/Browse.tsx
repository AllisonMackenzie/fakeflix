import React from 'react';

interface BrowseProps {}

export const Browse: React.FC<browseProps> = ({}) => {
  return (
    <React.Fragment>
      <div>Browse Page</div>
    </React.Fragment>
  );
};
