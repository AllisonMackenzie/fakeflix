import React from 'react';
import './Browse.css';

interface BrowseProps {}

export const Browse: React.FC<BrowseProps> = ({}) => {
  return (
    <React.Fragment>
      <div>Browse Page</div>
    </React.Fragment>
  );
};
